# ferrox-safetensors

SafeTensors mmap loader for Ferrox.

From-scratch reader for Hugging Face safetensors shards (header + raw
tensor bytes). Used for Kimi-style checkpoints that ship as safetensors
rather than GGUF.
